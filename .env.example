# Required: Your Drip API key
DRIP_API_KEY=drip_test_xxx

# Optional: Drip API URL (defaults to http://localhost:3001)
DRIP_API_URL=http://localhost:3001

# Optional: Use existing customer instead of creating one
TEST_CUSTOMER_ID=cust_xxx

# Optional: Skip cleanup of test data (for debugging)
SKIP_CLEANUP=false

# Optional: Timeout for each check in milliseconds (default: 30000)
CHECK_TIMEOUT=30000
